# 기업형 [모던 화이트 (Modern Light Pro)] 디자인이 적용

[theme]
# 메인 강조 색상 (버튼, 선택 탭, 슬라이더 등)
primaryColor = "#2563EB"

# 메인 화면 배경색 (눈이 편안한 오프화이트)
backgroundColor = "#F8FAFC"

# 사이드바 및 입력 상자 배경색
secondaryBackgroundColor = "#EFF6FF"

# 기본 글자 색상 (딥 차콜)
textColor = "#1E293B"

# 기본 폰트 스타일
font = "sans serif"